# 베이스라인·평가·데이터 작업 (CPU/API — 현재 로컬에서 사용)
openai>=1.40
python-dotenv>=1.0
pandas>=2.0
scikit-learn>=1.3
numpy>=1.24
tqdm>=4.66

# 파인튜닝 스택 (GPU PC 세팅 후 설치 — 버전은 GPU/CUDA에 맞춰 확정)
# torch            # CUDA 빌드
# transformers>=4.51    # Qwen3 아키텍처 지원 하한 (4.48 이하: "model type qwen3" 미인식으로 로드 실패)
# peft>=0.12
# trl>=0.9
# bitsandbytes>=0.43
# accelerate>=0.33
# datasets>=2.20
# sentencepiece
# tensorboard      # 무료·로컬 실험 추적 (REPORT_TO=tensorboard)
# wandb            # 선택 (클라우드 대시보드, 무료 한도 제한)
