# 베이스라인·평가·데이터 작업 (CPU/API — 현재 로컬에서 사용)
openai>=1.40
python-dotenv>=1.0
pandas>=2.0
scikit-learn>=1.3
numpy>=1.24
tqdm>=4.66

# 파인튜닝 스택 (GPU PC 세팅 후 설치 — 버전은 GPU/CUDA에 맞춰 확정)
# torch            # CUDA 빌드
# transformers>=4.44
# peft>=0.12
# trl>=0.9
# bitsandbytes>=0.43
# accelerate>=0.33
# datasets>=2.20
# sentencepiece
# wandb
